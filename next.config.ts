import type { NextConfig } from "next";


const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["http://127.0.0.1:8000/"], // Adicione os domínios necessários
  },
};

export default nextConfig;
